import { Layout } from 'components/layouts';
import { QuizForm } from 'components/organisms';

const Quiz = () => {
  return (
    <Layout title="Quiz">
      <QuizForm />
    </Layout>
  );
};

export default Quiz;
