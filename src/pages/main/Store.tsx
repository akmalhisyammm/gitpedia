import { Layout } from 'components/layouts';
import { StoreList } from 'components/organisms';

const Store = () => {
  return (
    <Layout title="Toko">
      <StoreList />
    </Layout>
  );
};

export default Store;
